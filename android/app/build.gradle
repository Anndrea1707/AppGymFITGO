plugins {
    id "com.android.application"
    id "kotlin-android"
    id "dev.flutter.flutter-gradle-plugin"
    id "com.google.gms.google-services"
}

dependencies {
    // Importar Firebase BoM (actualizado a la última versión recomendada)
    implementation platform('com.google.firebase:firebase-bom:33.8.0')

    // Dependencias de Firebase (no se especifican versiones por usar BoM)
    implementation 'com.google.firebase:firebase-analytics'

    // Agregar otras dependencias necesarias aquí...
}

android {
    namespace = "com.example.gym_fitgo"
    compileSdk = 35
    ndkVersion = flutter.ndkVersion

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_17
        targetCompatibility = JavaVersion.VERSION_17
    }

    kotlinOptions {
        jvmTarget = "17"
    }

    defaultConfig {
        applicationId = "com.example.gym_fitgo"
        minSdkVersion 23
        targetSdkVersion 34
        versionCode flutter.versionCode
        versionName flutter.versionName
    }

    buildTypes {
        release {
            signingConfig signingConfigs.debug
        }
    }
}

flutter {
    source = "../.."
}
